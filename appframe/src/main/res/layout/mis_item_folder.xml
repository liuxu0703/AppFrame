<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:background="@android:color/white"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/mis_item_folder_cover"
        android:layout_gravity="center_vertical"
        android:layout_width="@dimen/mis_folder_cover_size"
        android:layout_height="@dimen/mis_folder_cover_size"
        android:scaleType="centerCrop"
        android:src="@drawable/img_gallery_default"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_marginLeft="16dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/mis_item_folder_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            tools:text="img" />

        <TextView
            android:id="@+id/mis_item_folder_img_amount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:textSize="12sp"
            android:textColor="#AFAFAF"
            tools:text="1张" />

        </LinearLayout>

    <ImageView
        android:id="@+id/mis_item_folder_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/mis_ic_check"
        android:contentDescription="@null" />

</LinearLayout>